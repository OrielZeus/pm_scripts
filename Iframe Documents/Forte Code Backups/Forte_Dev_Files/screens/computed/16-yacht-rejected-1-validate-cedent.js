var cedent = "0";
if (this.YQP_REASSURED_CEDENT == "" ||
    this.YQP_REASSURED_CEDENT == null ||
    Object.keys(this.YQP_REASSURED_CEDENT).length == 0) { 
        cedent = "1";
        return cedent;
}
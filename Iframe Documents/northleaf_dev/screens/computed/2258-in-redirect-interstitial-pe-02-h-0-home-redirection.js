/**
 * Home Redirection
 * 
 * by Daniel Aguilar
*/
if(this.SUBMIT_02_PE == "SUBMIT" || this.SUBMIT_02_PE == "SAVE_CLOSE"){
    window.location.href = "/";
}

if(this.SUBMIT_PE_H == "SUBMIT" || this.SUBMIT_PE_H == "SAVE_CLOSE"){
    window.location.href = "/";
}
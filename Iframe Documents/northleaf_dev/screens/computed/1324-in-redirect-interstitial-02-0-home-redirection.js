debugger;
if (this.SUBMIT_02_INFRA == "SUBMIT") {
        setTimeout(() => {
            window.location.href = "/";
        }, "2000");
    }

    if (this.SUBMIT_02_PC == "SUBMIT") {
        setTimeout(() => {
            window.location.href = "/";
        }, "2000");
    }
    if (this.SUBMIT_02_PE == "SUBMIT") {
        setTimeout(() => {
            window.location.href = "/";
        }, "2000");
    }

    if (this.SUBMIT_02_INFRA == "BACK" && this.Allocated_INFRA == true) {
        setTimeout(() => {
            window.location.href = "/";
        }, "500");
    }

    if (this.SUBMIT_02_PC == "BACK" && this.Allocated_PC == true) {
        setTimeout(() => {
            window.location.href = "/";
        }, "500");
    }
    if (this.SUBMIT_02_PE == "BACK" && this.Allocated_PE == true) {
        setTimeout(() => {
            window.location.href = "/";
        }, "500");
    }
/*function LineItem(date, expenses, invoice, amount, glAccount) {
    this.date = date;
    this.expenses = expenses;
    this.invoice = invoice;
    this.amount = amount;
    this.glAccount = glAccount;
}

var arrayList = [];

var type = this.transactionTypeCode;

if(type == "ECA"){
    var ECA = this.ECA_recordList;

    if(ECA != undefined){
        for(var i = 0; i < ECA.length; i ++){
            var data = new LineItem(ECA[i].date, ECA[i].expenses, ECA[i].invoice, ECA[i].amount, ECA[i].glAccount)
            arrayList.push(data);
        }
    }
}

return arrayList;*/